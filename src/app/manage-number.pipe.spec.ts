import { ManageNumberPipe } from './manage-number.pipe';

describe('ManageNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new ManageNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
